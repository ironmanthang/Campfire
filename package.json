{
  "name": "campfire-monorepo",
  "private": true,
  "license": "UNLICENSED",
  "version": "0.1.12",
  "type": "module",
  "scripts": {
    "prepush": "node scripts/prepush.mjs",
    "version:bump": "node scripts/bump-version.mjs",
    "i18n:check": "pnpm --filter desktop --filter mobile i18n:check",
    "test": "pnpm --filter * test",
    "typecheck": "pnpm --filter * typecheck",
    "lint": "pnpm --filter desktop --filter mobile lint"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
