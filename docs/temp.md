
# request feature

in mobile when click on an entry, the app will have logic to set the cursor at the bottom right? i dont want it, i want the cursor to be disable, when they click on an entry, they can scroll the text if its too long, the cursor only set when user click on a text (it will set right at that place). when chose on a date picker i dont want the cursor to appear until user click on a text too. same thing for the arrow ford/backward date too. if too much text, the default view is at the top

i want the mobile app to remember the setting in filter entries across session, also make the filter in the middle, not bottom.

also the rotate didnt work, i rotate and the app still at potrait.
# questions

# errors